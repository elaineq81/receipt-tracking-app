# Receipt Archive web demo

This is a dependency-free interactive product preview for Vercel. It mirrors the native iPhone app's proposed navigation and review workflow using sample data only.

Open `index.html` directly for a quick local preview, or deploy this `web-demo` directory as a Vercel project. No receipt data is uploaded or persisted.

Live deployment: <https://receipt-tracking-app-lemon.vercel.app>
